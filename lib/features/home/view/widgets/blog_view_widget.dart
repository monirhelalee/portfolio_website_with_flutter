import 'package:flutter/material.dart';

class BlogViewWidget extends StatelessWidget {
  const BlogViewWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
